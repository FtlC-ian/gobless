terraform {
  backend "s3" {
    bucket       = "YOUR_TERRAFORM_STATE_BUCKET"
    key          = "gobless/terraform.tfstate"
    region       = "YOUR_AWS_REGION"
    use_lockfile = true
    encrypt      = true
  }
}
