# Lambda Error Response Fixtures

Expected sanitized error response shapes for Lambda/API Gateway tests. Each fixture includes `no_leak_assertions` strings that must not appear in serialized responses or logs.
