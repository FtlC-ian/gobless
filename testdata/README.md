# Test Fixtures

This tree contains sanitized fixtures for GoBless tests: golden SSH certificate vectors, test-only key placeholders, Lambda compatibility events, and policy catalogs.

Do not commit real private keys, production credentials, AWS secrets, or unsanitized customer/user data.
